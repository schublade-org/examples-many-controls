# Many controls

A single Filter bar story with text, select, number, and boolean controls — enough rows to scroll the right rail. The root catalog stays small; this is the place to stress the panel.

```bash
npx schublade serve --config ./schublade.toml
# or ./run.sh
```

http://127.0.0.1:47307
